import {
  Alert,
  AlertIcon,
  AlertTitle,
  AlertDescription,
  CloseButton,
  useDisclosure,
  Box,
} from "@chakra-ui/react";


const Alerta = ({ open }) => {
  console.log("🚀 ~ file: Alerta.js ~ line 14 ~ Alerta ~ open", open);
  

  return (
    <>
      {/* {isOpen ? ( */}
        
      {/* ) : (
        <Button
        // onClick={onOpen}
        >
          Show Alert
        </Button>
      )} */}
    </>
  );
};

export default Alerta;
